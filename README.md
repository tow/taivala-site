# Taivala website
Static landing page. Built as a single `index.html` with inline styles.
- Architecture & security snapshot (+ dedicated `trust.html`)
- Behavior-change examples
- No tracking, no JS frameworks
